#ifndef QUERIES_H
#define QUERIES_H

#include "graph_adj.h"

// Fonksiyon prototipleri (imzalarý)
void find_friends_events_photos(Graph* graph, int start_user_id);
void find_most_active_node(Graph* graph);

#endif
